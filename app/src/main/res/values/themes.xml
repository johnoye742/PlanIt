<resources>
    <!-- Base application theme. -->
    <style name="Theme.PlanIt" parent="@android:style/Theme.Material.Light.NoActionBar">
        <!-- Primary brand color. -->
        <item name="android:colorPrimary">@color/btn_color</item>

        <!-- Secondary brand color. -->

        <!-- Status bar color. -->
        <item name="android:statusBarColor">@color/btn_color</item>
        <!-- Customize your theme here. -->
        <item name="android:colorAccent">@color/pink</item>
        <item name="android:alertDialogTheme">@style/a1</item>
    </style>
    <style name="a1" parent="@android:style/Theme.Material.Light.Dialog.Alert">
       <item name="android:windowBackground">@drawable/d2</item>

    </style>
</resources>